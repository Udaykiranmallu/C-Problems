#include <stdio.h>

int main() {
    int balance;

    // Read account balance
    scanf("%d", &balance);

    // Check balance status
    if (balance < 1000) {
        printf("Low Balance");
    } else {
        printf("Sufficient Balance");
    }

    return 0;
}
