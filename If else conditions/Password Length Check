#include <stdio.h>

int main() {
    int length;

    // Read password length
    scanf("%d", &length);

    // Check if length is at least 8
    if (length >= 8) {
        printf("Strong Password");
    } else {
        printf("Weak Password");
    }

    return 0;
}
