#include <stdio.h>

int main() {
    int marks;

    // Read student marks
    scanf("%d", &marks);

    // Check pass or fail
    if (marks >= 40) {
        printf("Pass");
    } else {
        printf("Fail");
    }

    return 0;
}
