#include <stdio.h>

int main() {
    int num;

    // Read the number
    scanf("%d", &num);

    // Check number type
    if (num < 0) {
        printf("Negative");
    } else if (num == 0) {
        printf("Neutral");
    } else {
        printf("Positive");
    }

    return 0;
}
