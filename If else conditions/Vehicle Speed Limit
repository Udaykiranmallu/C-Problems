#include <stdio.h>

int main() {
    int speed;

    // Read vehicle speed
    scanf("%d", &speed);

    // Check speed limit
    if (speed > 80) {
        printf("Speed Limit Exceeded");
    } else {
        printf("Within Speed Limit");
    }

    return 0;
}
