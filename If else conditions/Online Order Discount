#include <stdio.h>

int main() {
    int amount;

    // Read purchase amount
    scanf("%d", &amount);

    // Check if discount applies
    if (amount >= 1000) {
        printf("Discount Applied");
    } else {
        printf("No Discount");
    }

    return 0;
}
