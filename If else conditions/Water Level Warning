#include <stdio.h>

int main() {
    int level;

    // Read water level percentage
    scanf("%d", &level);

    // Check water level
    if (level > 90) {
        printf("Overflow Warning");
    } else {
        printf("Safe Level");
    }

    return 0;
}
