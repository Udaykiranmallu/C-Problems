#include<stdio.h>
int main(){
    int category,distance;
    scanf("%d",&category);
    scanf("%d",&distance);
    

    switch(category){
        case 1:
        if(distance<=30)
        printf("Eligible");
        else
        printf("Eligible with extra fee");
        break;

        case 2:
        if(distance<=30)
        printf("Eligible");
        else
        printf("Not eligible");
        break;

    }
return 0;
}
