#include <stdio.h>
int main() {
   int a,n;
   int OverspendDays=0;
   int sum=0;
scanf("%d",&n);
for(int i=0;i<n;i++){
    scanf("%d",&a);
    sum+=a;
    if(a>1000){
        OverspendDays++;
    }
}
printf("%d\n",sum);
printf("%d",OverspendDays);

    return 0;
}
