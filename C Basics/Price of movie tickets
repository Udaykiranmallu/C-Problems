#include <stdio.h>

int main() {
    int age;

    // Read age
    scanf("%d", &age);

    // Determine ticket type
    if (age < 12) {
        printf("Discount Ticket");
    } else {
        printf("Regular Ticket");
    }

    return 0;
}
