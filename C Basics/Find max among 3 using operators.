#include <stdio.h>
int main() {
int a,b,c;
scanf("%d %d %d",&a,&b,&c);
if(a>b&&a>c){
    printf("%d is greatest");

}else if(b>a&&b>c){
    printf("%d is greatest");
}else{
    printf("%d is greater");
}
    
    return 0;
}
